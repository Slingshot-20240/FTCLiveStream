export enum State {
    SCORING, RESULTS, ALLIANCE_SELECTION, AWARDS, ADVANCEMENTS, BANNER
}

export enum MatchState {
    PREVIEW, AUTO, AUTO_END, TRANSITION, TELEOP, ENDGAME, FINISHED
}

export enum ResultsState {
    BASE, UP_NEXT
}

export enum BannerState {
    TEXT, PRESENTATION
}